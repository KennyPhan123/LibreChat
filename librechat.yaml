version: 1.0.0
endpoints:
  custom:
    - name: "Unify"
      apiKey: "${UNIFY_API_KEY}"
      baseURL: "https://api.unify.ai/v0"
      models:
        fetch: true
        default: ["o1@openai", "o3-mini@openai"]
      modelDisplayLabel: "Unify"
      dropParams: ["stop", "user", "frequency_penalty", "presence_penalty"]
